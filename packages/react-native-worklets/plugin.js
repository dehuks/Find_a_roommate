// No-op Babel plugin stub for react-native-worklets/plugin
// nativewind/babel requires this, but we don't need the worklets runtime
// since we're using react-native-reanimated v3 (not v4)
module.exports = function () {
    return { visitor: {} };
};
